/// type of request : [RequestType.POST] or [RequestType.GET]
enum RequestType { get, post, put, delete, uploadFile }

enum ParametersType { body, url }

enum DeviceScreenType { mobile, tablet, desktop }

enum UploadState { init, loading, done, error, canceled }

enum Buttons { instagram, facebook, whatsApp, mobile, phone }
